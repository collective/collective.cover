<html xmlns="http://www.w3.org/1999/xhtml"
      xmlns:metal="http://xml.zope.org/namespaces/metal"
      xmlns:tal="http://xml.zope.org/namespaces/tal"
      xmlns:i18n="http://xml.zope.org/namespaces/i18n"
      i18n:domain="collective.cover">

<body tal:define="tile_id view/id;
                  autoplay python:str(view.autoplay()).lower();">
  <div class="galleria slide"
       tal:attributes="id string:galleria-$tile_id">
    <!-- Carousel items -->
    <div class="galleria-inner sortable-tile">
      <tal:items repeat="item view/results">
              <img tal:define="scales item/@@images;
                               thumbnail python:scales.scale('image', width=1000, height=1000);
                               uid item/UID|nothing;"
                   tal:condition="python:thumbnail and uid"
                   tal:attributes="src thumbnail/url;
                                   width thumbnail/width;
                                   height thumbnail/height;
                                   data-title item/title;
                                   data-description item/Description;
                                   data-uid uid;" />
      </tal:items>
    </div>

  </div>

  <link rel="stylesheet" type="text/css"
        tal:attributes="href string:${context/@@plone_portal_state/portal_url}/++resource++collective.cover/galleria-theme/galleria.cover_theme.css" />

  <script type="text/javascript">
	Galleria.loadTheme("++resource++collective.cover/galleria-theme/galleria.cover_theme.js");
	<tal:configure replace="string:Galleria.run('#galleria-$tile_id .galleria-inner');" />

	var rm_button = function(data) {
	    var uid = data.original.attributes['data-uid'].value;
	    return "<i class='tile-remove-item' data-uid='" + uid + "'><span class='text'>remove</span></i>";
	};

    function carousel_init() {
		if($('body').hasClass('template-view')) {
			<tal:configure replace="string:Galleria.configure({ autoplay: $autoplay });" />
		};
    };
    carousel_init();
  </script>
</body>
</html>
