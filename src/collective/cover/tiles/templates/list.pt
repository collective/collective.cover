<html xmlns="http://www.w3.org/1999/xhtml"
      xmlns:metal="http://xml.zope.org/namespaces/metal"
      xmlns:tal="http://xml.zope.org/namespaces/tal"
      xmlns:i18n="http://xml.zope.org/namespaces/i18n"
      i18n:domain="collective.cover">

<body tal:define="is_empty view/is_empty">
  <p tal:condition="is_empty" i18n:translate="">
    Please add up to <span i18n:name="limit_objects" tal:replace="view/limit"/> objects to the tile.
  </p>

  <div class="sortable-tile cover-list-tile"
       tal:condition="not:is_empty">

    <tal:items define="toLocalizedTime nocall:context/@@plone/toLocalizedTime;
                       fields view/get_configured_fields"
               repeat="item view/results">
      <div class="list-item" data-uid=""
           tal:attributes="data-uid python:view.get_uid(item)">

        <tal:fields repeat="field fields">
          <tal:title condition="python:field['id'] == 'title'">
            <h1 tal:replace="structure python:view.title_tag(item)">
               <a href="" title="">Title</a>
            </h1>
          </tal:title>

          <tal:description condition="python:field['id'] == 'description'">
            <p tal:content="python:view.description(item)">Description</p>
          </tal:description>

          <tal:image condition="python:field['id'] == 'image'">
            <a class="imag" href="" title=""
               tal:replace="structure python:view.image_tag(item)">
              <img src="" width="" height="" class="left" alt="" />
            </a>
          </tal:image>

          <tal:date condition="python:field['id'] == 'date'">
            <p tal:content="python:toLocalizedTime(item.Date(), True)">Date</p>
          </tal:date>
        </tal:fields>
      </div>

      <div class="visualClear"><!-- --></div>
    </tal:items>
    <div class="visualClear"><!-- --></div>
  </div>
</body>
</html>
